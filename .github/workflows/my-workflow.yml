#Testing-32
name: Run Update

on:
  push:
    branches:
      - use-action

jobs:
  action:
    name: Call remote action
    runs-on: ubuntu-latest
    steps:
      - name: Emit repository_dispatch
        uses: leethomasrh/cop-bazaar@action-yaml
        with:
          input_path: ./cop
          output_path: ./cop-output
